export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  BANNED = 'BANNED',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  GUEST = 'GUEST',
}

export enum FrontendType {
  SELECT = 'SELECT',
  RADIO = 'RADIO',
  TEXT = 'TEXT',
  TEXT_AREA = 'TEXT_AREA',
}
